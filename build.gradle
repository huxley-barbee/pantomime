apply plugin: 'java'
apply plugin: 'groovy'

dependencies{
    compile fileTree(dir: 'lib', include: '*')

}

jar.baseName = 'pantomime-0.9984'

jar {
    from "LICENSE"
}

task loadMySQL(type:Exec) {

    executable = 'mysql'
    args = ['-u', 'root', 'pantomime']
    standardInput = file("data/pantomime.sql").newInputStream()

}

test {
    systemProperties = [
        'log4j.debug': ''
    ]

    classpath += files('etc')
}

test.dependsOn loadMySQL
